using MaKore.Models;

namespace MaKore.Services
{
    public class UserService
    {


        //private static List<User> users = new List<User>();

//        public List<User> GetUsers() { return users;  }

//        public User GetUser(string userName) { return users.Find(x => x.UserName == userName); }

        //public void CreateUser(string userName, string nickName, string password) { users.Add(new User {UserName = userName, NickName = nickName,, Password = password}); }
        
    }
}
